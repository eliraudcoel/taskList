<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appy.technictasks">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBSv6w7tB02up_Z3os81NF-lpPmgv2b1fQ" />

        <activity android:name=".Controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controllers.ListTasks" />
        <activity android:name=".Controllers.TaskDetail" />
        <activity android:name=".Controllers.EditTask"></activity>
        <activity android:name=".Controllers.CalendarPicker"></activity>
        <activity android:name=".Controllers.TimerPicker"></activity>

    </application>

</manifest>
